// Marker component — any room prefab with this on its root is treated as a hub sandbox
// where no player resources are consumed and no permanent state changes occur.
using UnityEngine;

public class HubMarker : MonoBehaviour { }
